# 農園名ログインへの変更

## ログイン方法

ログインIDをメールアドレスから農園名へ変更しました。

```text
農園名 + パスワード
```

初回登録で入力するメールアドレスは、購入通知・万引き通知・動画通知などの初期通知先として使用します。

## データの単位

農園ごとに1つの `shop_id` を発行し、次のデータを農園単位で保存します。

- 商品・在庫
- 売上履歴
- 通知履歴
- メール通知先

同じ農園名を新しく重複登録することはできません。追加の通知先は、ログイン後の設定画面から登録します。

## 旧版データとの互換性

旧版の `runtime/users.json` がメールアドレスをキーにしていても、保存されている `shop_name` から農園を検索してログインできます。

旧版で同じ農園名が複数の異なる `shop_id` として登録されている場合は、自動的にどのデータを使うか決めず、ログインを停止してデータ統合を促します。
